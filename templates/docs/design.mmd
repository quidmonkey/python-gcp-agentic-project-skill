%% Architecture diagram for docs/design.md.
%% Once the design splits into per-flow specs, each flow gets its own
%% docs/specs/<flow>-diagram.mmd and this one stays at the system level.
graph TD
    A[{{project-name}}] --> B[Component]
